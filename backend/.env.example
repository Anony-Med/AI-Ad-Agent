# Application Settings
DEBUG=true

# JWT Authentication (change in production!)
# JWT_SECRET_KEY=change-me-in-production

# ============================================================================
# LOCAL DEVELOPMENT SETUP:
# ============================================================================
# 1. Authenticate with GCP using Application Default Credentials (ADC):
#    gcloud auth application-default login
#    gcloud config set project sound-invention-432122-m5
#
# 2. That's it! The app will automatically use Secret Manager with your ADC.
#    No need to set USE_SECRET_MANAGER=false or download service account keys.
#
# For production (Cloud Run):
# 1. Uses Workload Identity (also ADC) - no manual config needed
# 2. Ensure secrets exist in Secret Manager.
# ============================================================================
